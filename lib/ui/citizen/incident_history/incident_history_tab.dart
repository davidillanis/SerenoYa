import 'package:flutter/material.dart';

class IncidentHistoryTab extends StatelessWidget {
  const IncidentHistoryTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Historial de Incidencias pasadas'),
    );
  }
}
